Usuario <abstract>
        # nome: String
        # cpf: String
        # senha: String
        Usuario (String: nome, String: cpf, String: senha)
        setNome();
        getNome();
        Autenticar(): boolean

Cliente extends Usuario
        Cliente (String nome...)
            super(nome, cpf, senha)

Funcionario (abstrata) extends Usuario
        # cargo: Cargo //enum
        Funcionario(String cpf, String senha)

Gerente extends Funcionario
        # idAgencia: Agencia //enum
        Gerente(String nome, String cpf, String senha, String cargo, String idAgencia)

Diretor extends Funcionario
        Diretor(String nome, String cpf, String senha, String cargo)

Presidente extends Funcionario
        Presidente(String nome, String cpf, String senha, String cargo)


Conta (abstrata)
        # cpfTitular: Cliente
        # numeroConta : int
        # idAgencia: Agencia //enum
        - saldo : double
        getNumeroConta(): int
        getIdentificadorAgencia(): int
        getSaldo(): double
        depositar(double valor): void
        sacar(double valor): void
        transferir(double valor, Conta destino): boolean

ContaCorrente extends Conta
        - tipo: String

ContaPoupanca extends Conta
        - tipo: String


public enum Cargo{
    GERENTE, DIRETOR, PRESIDENTE
}

public enum Agencia{
    A01, A02, A03
}
